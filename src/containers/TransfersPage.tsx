import { Box, Typography } from '@mui/material'

export default function TransfersPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h5">Ward Transfers</Typography>
      <Typography color="text.secondary" sx={{ mt: 1 }}>Transfers — coming soon</Typography>
    </Box>
  )
}
