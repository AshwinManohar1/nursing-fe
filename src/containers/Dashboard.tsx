import { Box, Typography } from '@mui/material'

export default function Dashboard() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h5">Ward Overview</Typography>
      <Typography color="text.secondary" sx={{ mt: 1 }}>Dashboard — coming soon</Typography>
    </Box>
  )
}
