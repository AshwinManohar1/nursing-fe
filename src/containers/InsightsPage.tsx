import { Box, Typography } from '@mui/material'

export default function InsightsPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h5">Compliance & Analytics</Typography>
      <Typography color="text.secondary" sx={{ mt: 1 }}>Insights — coming soon</Typography>
    </Box>
  )
}
