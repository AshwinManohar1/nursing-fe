import { Box, Typography } from '@mui/material'

export default function RosterPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h5">Weekly Roster</Typography>
      <Typography color="text.secondary" sx={{ mt: 1 }}>Roster editor — coming soon</Typography>
    </Box>
  )
}
